name: flutter_security_suite
description: "Flutter plugin for mobile app security: root/jailbreak detection, certificate pinning, app integrity checks, screenshot protection, and encrypted secure storage."
version: 1.0.4
homepage: https://github.com/DeepakPal25/flutter_security_suite
repository: https://github.com/DeepakPal25/flutter_security_suite
issue_tracker: https://github.com/DeepakPal25/flutter_security_suite/issues
license: MIT
topics:
  - security
  - encryption
  - root-detection
  - certificate-pinning
  - storage

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mocktail: ^1.0.4

flutter:
  plugin:
    platforms:
      android:
        package: com.securebankkit.flutter_security_suite
        pluginClass: SecureBankKitPlugin
      ios:
        pluginClass: SecureBankKitPlugin
